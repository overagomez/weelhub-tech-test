services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: wheelhub
      POSTGRES_PASSWORD: wheelhub
      POSTGRES_DB: wheelhub
    ports:
      - "5432:5432"
  api:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    depends_on:
      - db